# Optional release identifier, such as a Git commit SHA or app version.
VITE_SENTRY_RELEASE=

# Production build-only variables for Sentry release creation and source-map upload.
# Keep SENTRY_AUTH_TOKEN secret; never prefix it with VITE_.
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
